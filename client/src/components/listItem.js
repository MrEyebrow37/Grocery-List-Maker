import {useState,useEffect} from 'react'

const ListItem = ({item,state,functions}) => {
    

    return (
        <div className={`listItem`}>
            <p>item</p>
        </div>
    )
}

export default ListItem